node_modules/
.env
*.log
dist/

# Yarn PnP — .pnp.cjs/.pnp.loader.mjs regenerated by `yarn install`, not committed.
# .yarn/sdks and .yarn/releases ARE committed (editor TS support, pinned yarn binary) —
# only the local/regenerable state below is ignored.
.pnp.cjs
.pnp.loader.mjs
.yarn/cache
.yarn/unplugged
.yarn/install-state.gz

kb-summaries.md

.DS_Store
